"""
Пилот-приложение magnet-platform — обёртка над верифицированным ядром `magcore`
для запуска расчётов, визуализации и ручной проверки результатов.

ГРАНИЦА: пилот ТОЛЬКО импортирует `magcore` (read-only) и НЕ изменяет его —
верифицированное ядро (435 тестов) остаётся неприкосновенным. Всё приложение
(CLI, конфиги, визуализация, сценарии) живёт здесь; удаление папки `pilot/`
не затрагивает ядро. Переиспользуемая физика (момент/ЭДС/потери/мешер) при
созревании поднимается в `magcore` аддитивно, с собственными оракулами.

См. project memory (xxiii): пилот v1 (реальная геометрия + пост-проц + CLI).
"""
